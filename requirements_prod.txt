# NexVision IPTV — Production Dependencies
# Install: pip install -r requirements_prod.txt
# Updated: 2026-05-08 - security bump for Dependabot alerts #27 #31-#44
# Entry points: app/wsgi.py (gunicorn) or run.py (dev)

# ── Core Flask ────────────────────────────────────────────────────────────────
Flask==3.1.3
Werkzeug==3.1.6
Jinja2==3.1.6
MarkupSafe==2.1.5
itsdangerous==2.2.0
blinker==1.8.2
click==8.1.8
flask-cors==6.0.2
PyJWT==2.10.1

# ── Production WSGI server ────────────────────────────────────────────────────
gunicorn==23.0.0
gevent==24.2.1              # async worker for 500+ concurrent connections
greenlet==3.1.1             # gevent dependency
zope.event==5.0
zope.interface==7.2

# ── MySQL (replaces sqlite3 in production) ────────────────────────────────────
PyMySQL==1.1.2
cryptography==47.0.0        # PyMySQL auth plugin support
cffi==1.17.1
pycparser==2.23

# ── Redis caching ─────────────────────────────────────────────────────────────
Flask-Caching==2.3.1
cachelib==0.13.0
redis==6.1.1

# ── Environment config ────────────────────────────────────────────────────────
python-dotenv==1.2.2

# ── HTTP client ───────────────────────────────────────────────────────────────
requests==2.33.0
urllib3==2.7.0
certifi==2026.4.22
charset-normalizer==3.4.7
idna==3.13

# ── packaging ─────────────────────────────────────────────────────────────────
packaging==26.2

# ── Optional cloud storage (install separately if needed) ─────────────────────
# boto3                     # AWS S3 support
# azure-storage-blob        # Azure Blob support
# google-cloud-storage      # GCS support
